package com.haidrrrry.backgroundremover.utils

/**
 * Enum class to define the different display modes for images.
 */
enum class DisplayMode {
    NORMAL,     // Represents the normal image view
    MASK,       // Represents the image mask view
    CUSTOM_BG   // Represents the image with a custom background
}